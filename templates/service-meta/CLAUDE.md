# <서비스명> 워크스페이스 (메타 레포)

<서비스 한 줄 설명>

이 디렉터리가 Claude Code 세션의 진입점이다. 이 파일은 항상 로드되고,
하위 레포의 CLAUDE.md는 그 레포의 파일을 읽는 순간 자동 로드된다.

## 레포 구성

| 레포 | 책임 |
|---|---|
| `<repo-1>/` | <한 줄 설명> |
| `<repo-2>/` | <한 줄 설명> |

## 서비스 규칙

- <레포 간 의존·호출 관계>
- <사내 용어, 헷갈리기 쉬운 단어>
- <공통 컨벤션 — 커밋, 리뷰, 배포>

## 영역 구분 — 새 자산(스킬·에이전트·규칙)을 만들 때

- 팀원과 공유할 것 → 해당 제품 레포의 `CLAUDE.md`·`.claude/`에 커밋, 또는 팀 플러그인
- 이 서비스에서 나만 쓸 것 → 이 레포의 `.claude/skills|agents/`
- 팀·서비스 무관한 개인 취향 → `~/.claude/` (원본은 `~/my-settings/claude/`에 반영)
- 개인 규칙·임시 실험 → `.claude/settings.local.json`·`CLAUDE.local.md` (커밋 금지)
